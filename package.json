{
  "name": "task_manager",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/app.js",
    "heroku-postbuild": "cd frontend/my-app && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
